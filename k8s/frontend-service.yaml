apiVersion: v1
kind: Service
metadata:
  name: genai-frontend
spec:
  selector:
    app: genai-frontend
  ports:
  - protocol: TCP
    port: 3000           # External port exposed by Kubernetes
    targetPort: 3000   # Internal port your container listens on
    nodePort: 30501  # Optional: fixed port
  type: NodePort