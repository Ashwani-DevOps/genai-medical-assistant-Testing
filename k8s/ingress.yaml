apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: genai-ingress
spec:
  rules:
  - host: genai.local
    http:
      paths:
      - path: /backend
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
      - path: /frontend
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80